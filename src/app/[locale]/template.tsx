/**
 * 페이지 전환은 브라우저 네이티브 View Transitions(크로스페이드)로 처리한다.
 * (ViewTransitionNav + globals.css ::view-transition)
 */
export default function Template({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
